<%= form_with(model: project) do |f| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited the project from being saved:</h2>
      <ul>
        <% project.errors.full_messages.each do |message| %>
          <li><%= message %></li >
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, style: "display: block" %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Update Project" %>
  </div>
<% end %>